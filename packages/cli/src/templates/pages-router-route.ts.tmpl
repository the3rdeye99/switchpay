import { buildPagesHandler, config } from "switchpay/next";

// Handles /api/switchpay/init, /api/switchpay/verify, and /api/switchpay/webhook.
// To hook into payment lifecycle events, pass callbacks to buildPagesHandler():
//
// export default buildPagesHandler({
//   onPaymentSuccess: async (tx) => { /* e.g. mark order paid in your DB */ },
//   onPaymentFailed: async (tx) => { /* e.g. notify the customer */ },
// });

export { config };
export default buildPagesHandler();
